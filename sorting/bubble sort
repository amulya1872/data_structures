def bubblesort(l):
    for i in range(0,len(l)):
        for j in range(i+1,len(l)):
            if(l[i]>l[j]):
                (l[i],l[j])=(l[j],l[i])

    return(l)
l=[int(x) for x in input().split()]
print(bubblesort(l))
